# This file is managed by macpro-mdct-core so if you'd like to change it let's do it there
name: Audit Account

on:
  schedule:
    # Every Monday at 1600 UTC
    - cron: "0 16 * * 1"
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.event.ref }}
  cancel-in-progress: false

permissions:
  id-token: write

jobs:
  audit:
    name: Audit
    environment: dev
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Checkout macpro-mdct-tools repository
        uses: actions/checkout@v5
        with:
          repository: Enterprise-CMCS/macpro-mdct-tools
          path: macpro-mdct-tools

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version-file: ".nvmrc"

      - name: Enable Corepack
        run: corepack enable

      - name: Set branch_name
        run: |
          BRANCH_NAME=$(./.github/setBranchName.ts ${{ github.ref_name }})
          echo "branch_name=${BRANCH_NAME}" >> $GITHUB_ENV

      - name: Configure AWS credentials for GitHub Actions
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_OIDC_ROLE_TO_ASSUME }}
          aws-region: us-east-1

      - name: Cache Node modules and Cypress folder
        uses: actions/cache@v4
        with:
          path: |
            **/node_modules
            ~/.cache/Cypress
          key: ${{ runner.os }}-modules-${{ hashFiles('**/yarn.lock', 'plugins/**') }}

      - name: Set path
        run: |
          echo "PATH=$(pwd)/node_modules/.bin/:$PATH" >> $GITHUB_ENV

      - name: List all topics for project
        run: |
          # Executing the run command is required to assemble all the dependencies in the topics service
          ./run list-topics --stage $branch_name
        env:
          PROJECT: ${{ vars.APP_NAME_LOWER }}

      - name: Collect resources from account
        run: pushd .github && aws resourcegroupstaggingapi get-resources > resources.json

      - name: List orphaned resources created by CI pipeline
        run: pushd .github && ./auditAccount.sh ci_inactive resources.json

      - name: List untagged resources
        run: pushd .github && ./auditAccount.sh untagged resources.json

      - name: List orphaned topics
        run: pushd .github && ./auditAccount.sh orphaned_topics

      - name: Create reports dir
        run: pushd .github && mkdir -p reports

      - name: Install macpro-mdct-tools dependencies
        run: |
          pushd macpro-mdct-tools/scripts
          yarn install --immutable
          popd

      - name: Run unmanaged resources scan
        run: |
          set -euo pipefail
          pushd macpro-mdct-tools/scripts
          ./scanResources.ts || { echo "scanResources.ts failed"; exit 1; }
          popd
          set +e

          REPORT_FILE=$(ls macpro-mdct-tools/scripts/unmanaged-resources-*.txt 2>/dev/null | grep -v -- '-delete-commands\.txt$' | head -n1 || true)
          if [ -n "${REPORT_FILE:-}" ]; then
            echo "Found unmanaged resources report: $REPORT_FILE"
            cp "$REPORT_FILE" .github/reports/ || echo "Warning: Failed to copy report file"
          else
            echo "No unmanaged resources report generated."
          fi

          DELETE_FILE=$(ls macpro-mdct-tools/scripts/unmanaged-resources-*-delete-commands.txt 2>/dev/null | head -n1 || true)
          if [ -n "${DELETE_FILE:-}" ]; then
            echo "Found unmanaged resources delete commands: $DELETE_FILE"
            cp "$DELETE_FILE" .github/reports/ || echo "Warning: Failed to copy delete commands file"
          else
            echo "No delete commands file generated (likely no unmanaged deletables found)."
          fi
        env:
          GITHUB_TOKEN: ${{secrets.GITHUB_TOKEN}}

      - name: Assemble CSV files
        run: |
          #!/bin/bash
          pushd .github
          echo "Reports with no entries will be omitted"
          CI_INACTIVE="$(./auditAccount.sh ci_inactive resources.json)"
          [[ $(jq -r 'length' <<< "${CI_INACTIVE}") -gt 0 ]] && jq -r '(.[0]
            | keys_unsorted) as $keys | $keys, map([.[ $keys[] ]])[] | @csv' <<< "${CI_INACTIVE}" > reports/ci_inactive.csv || :
          UNTAGGED="$(./auditAccount.sh untagged resources.json)"
          [[ $(jq -r 'length' <<< "${UNTAGGED}") -gt 0 ]] && jq -r '(.[0]
            | keys_unsorted) as $keys | $keys, map([.[ $keys[] ]])[] | @csv' <<< "${UNTAGGED}" > reports/untagged.csv || :
          TOPICS="$(./auditAccount.sh orphaned_topics)"
          [[ $(jq -r 'length' <<< "${TOPICS}") -gt 0 ]] && jq -r '(.[0]
            | keys_unsorted) as $keys | $keys, map([.[ $keys[] ]])[] | @csv' <<< "${TOPICS}" > reports/orphaned_topics.csv || :

      - name: Upload reports
        uses: actions/upload-artifact@v4
        with:
          name: resource-reports
          path: .github/reports/
          retention-days: 14
