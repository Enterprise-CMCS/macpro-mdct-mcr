service: database

frameworkVersion: "2"

plugins:
  - serverless-dynamodb-local
  - serverless-stack-termination-protection
  - serverless-s3-bucket-helper

s3BucketHelper:
  loggingConfiguration:
    destinationBucketName: ${ssm:/configuration/${self:custom.stage}/s3/accessLogsBucket~true, ssm:/configuration/default/s3/accessLogsBucket~true}
    logFilePrefix: ${ssm:/configuration/${self:custom.stage}/s3/accessLogsPrefix~true, ssm:/configuration/default/s3/accessLogsPrefix~true}

custom:
  stage: ${opt:stage, self:provider.stage}
  region: ${opt:region, self:provider.region}
  serverlessTerminationProtection:
    stages:
      - main
      - val
      - production
  bannerTableName: ${self:custom.stage}-banners
  reportDataTableName: ${self:custom.stage}-mcpar-reports-data
  reportTableName: ${self:custom.stage}-mcpar-reports
  formTableName: ${self:custom.stage}-forms
  dynamodb:
    stages:
      - local
    start:
      port: 8000
      inMemory: true
      migrate: true

provider:
  name: aws
  runtime: nodejs14.x
  region: us-east-1

resources:
  Resources:
    BannerTable:
      Type: AWS::DynamoDB::Table
      Properties:
        TableName: ${self:custom.bannerTableName}
        PointInTimeRecoverySpecification:
          PointInTimeRecoveryEnabled: true
        StreamSpecification:
          StreamViewType: NEW_AND_OLD_IMAGES
        AttributeDefinitions:
          - AttributeName: key
            AttributeType: S
        KeySchema:
          - AttributeName: key
            KeyType: HASH
        BillingMode: PAY_PER_REQUEST # Set the capacity to auto-scale
    ReportDataTable:
      Type: AWS::DynamoDB::Table
      Properties:
        TableName: ${self:custom.reportDataTableName}
        PointInTimeRecoverySpecification:
          PointInTimeRecoveryEnabled: true
        StreamSpecification:
          StreamViewType: NEW_AND_OLD_IMAGES
        AttributeDefinitions:
          - AttributeName: state
            AttributeType: S
          - AttributeName: reportId
            AttributeType: S
        KeySchema:
          - AttributeName: state
            KeyType: HASH
          - AttributeName: reportId
            KeyType: RANGE
        BillingMode: PAY_PER_REQUEST # Set the capacity to auto-scale
    ReportTable:
      Type: AWS::DynamoDB::Table
      Properties:
        TableName: ${self:custom.reportTableName}
        PointInTimeRecoverySpecification:
          PointInTimeRecoveryEnabled: true
        StreamSpecification:
          StreamViewType: NEW_AND_OLD_IMAGES
        AttributeDefinitions:
          - AttributeName: state
            AttributeType: S
          - AttributeName: reportId
            AttributeType: S
        KeySchema:
          - AttributeName: state
            KeyType: HASH
          - AttributeName: reportId
            KeyType: RANGE
        BillingMode: PAY_PER_REQUEST # Set the capacity to auto-scale
    FormTable:
      Type: AWS::DynamoDB::Table
      Properties:
        TableName: ${self:custom.formTableName}
        PointInTimeRecoverySpecification:
          PointInTimeRecoveryEnabled: true
        StreamSpecification:
          StreamViewType: NEW_AND_OLD_IMAGES
        AttributeDefinitions:
          - AttributeName: formName
            AttributeType: S
          - AttributeName: formId
            AttributeType: S
        KeySchema:
          - AttributeName: formName
            KeyType: HASH
          - AttributeName: formId
            KeyType: RANGE
        BillingMode: PAY_PER_REQUEST # Set the capacity to auto-scale
  Outputs:
    BannerTableName:
      Value: !Ref BannerTable
    BannerTableArn:
      Value: !GetAtt BannerTable.Arn
    BannerTableStreamArn:
      Value: !GetAtt BannerTable.StreamArn
    ReportDataTableName:
      Value: !Ref ReportDataTable
    ReportDataTableArn:
      Value: !GetAtt ReportDataTable.Arn
    ReportDataTableStreamArn:
      Value: !GetAtt ReportDataTable.StreamArn
    ReportTableName:
      Value: !Ref ReportTable
    ReportTableArn:
      Value: !GetAtt ReportTable.Arn
    ReportTableStreamArn:
      Value: !GetAtt ReportTable.StreamArn
    FormTableName:
      Value: !Ref FormTable
    FormTableArn:
      Value: !GetAtt FormTable.Arn
    FormTableStreamArn:
      Value: !GetAtt FormTable.StreamArn
    Region:
      Value: !Sub ${AWS::Region}
