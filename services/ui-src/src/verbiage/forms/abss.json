{
  "path": "/program-information/add-bss-entities",
  "intro": {
    "section": "Section A: Program Information",
    "subsection": "Add BSS entities (A.8)",
    "info": "Enter the names of Beneficiary Support System (BSS) entities that support enrollees in the program for which the state is reporting data."
  },
  "form": {
    "id": "abssentities",
    "options": {
      "mode": "onChange"
    },
    "fields": [
      {
        "id": "abss-a8",
        "type": "text",
        "props": {
          "label": "BSS entity name"
        },
        "validation": {
          "type": "string",
          "options": {
            "required": true
          },
          "errorMessages": {
            "required": "BSS entity name is required"
          }
        }
      }
    ]
  }
}
